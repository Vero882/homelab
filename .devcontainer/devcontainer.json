{
  "build": {
    "context": "..",
    "dockerfile": "Dockerfile"
  },
  "mounts": [
    "source=${env:HOME}/.kube/config,target=/home/vscode/.kube/config,type=bind"
  ],
  "remoteEnv": {
    "KUBECONFIG": "/home/vscode/.kube/config"
  },
  "postCreateCommand": "scripts/setup"
}
