apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: synology-csi-secret
  namespace: synology-csi
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-kv-store
    kind: ClusterSecretStore
  data:
    - secretKey: client-info.yml
      remoteRef:
        key: SynCSI-client-info
